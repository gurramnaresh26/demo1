pipeline{
     	  agent
    		{
    		 label "slave"
		}
    stages {
        
        stage('clone') {
            steps{
            git 'https://github.com/Apuroopa4/demo1.git'
        }
        }
    }
}
